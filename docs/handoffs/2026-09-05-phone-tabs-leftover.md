# Dashboard handoff: phone tabs leftover honesty

Snapshot: 2026-09-05, America/Chicago. Source-only product round.

## Product delta

The first phone screen still has one next action. The type tab bar no longer
gives leftover-only types equal weight next to live work. Live types plus
Parked stay on the first-screen row. Types whose every row is parked leftover
or Jeff-gate (today Cisco and Media) sit under Parked as leftover links and
carry an honest leftover banner. They are not active agents and not a Jeff
yes. Opening a leftover type from Parked, a hash, or a later live-work glance
temporarily puts that type back on the tab bar so the panel can close.

Existing type panels stay in the document. Vault stays in Live. Parked leftover
drafts #12 and #21 were not merged or rewritten. Possession of the public URL
remains the trust boundary; there is no Unlock / OTP.

## Verification and reproduction

Run `python3 qa-offline.py` from the source checkout. That is the pull-request
QA entry point. It builds marked synthetic artifacts, runs the full claim
smoke, and must leave scheduler-owned `index.html` / `status.json` byte-stable.
Hosted exact-tip evidence belongs in the PR; these results do not claim this
source is published.

## Authority and remaining limits

- Draft only for Karen. No merge, tag, release, deploy, Pages dispatch, or
  scheduler mutation without Jeff.
- Parked leftover drafts #12 and #21 remain open drafts.
- Scheduled publication materializes generated files after source lands.

## Next safe step

Karen reviews the exact draft tip, first-screen tab presentment, leftover
switcher, and the unchanged OTP / parked-draft / source-only boundary. Only
after explicit exact-head landing approval should the scheduled refresh
materialize the source.

## Privacy check

No private repository metadata, customer data, creative details, credentials,
owner probes, or local paths are added to this handoff or the public product.
