# bob-ops-dashboard

Public status board for Jeff Story's live projects (Bob).

- GitHub Pages serves `index.html`
- `status.json` is the machine-readable snapshot
- No secrets / no customer data — AdoptIQ/TACTrack are high-level only

Refresh: regenerate from `gh` status and push to `main`.
